[project]
name = "DirCheckSum"
version = "0.1.0"
description = "目录哈希校验工具。计算目录下所有文件的哈希校验和，报告前后目录下文件变化。"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyinstaller>=6.20.0",
    "pytest>=9.0.3",
]
